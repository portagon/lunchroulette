= form_with model: @user, url: users_path, local: true, html: { class: 'margin' } do |f|
  .margin
    = f.label :email
    = f.text_field :email, placeholder: 'y.yourname@portagon.com', autofocus: true
    = f.render_errors :email

  .margin
    = f.submit 'Login', class: 'primary button-w100'
